# Hex Tripwire Test

One test that recursively scans `src/domain/` for forbidden
imports/usages: `cbor-x`, `defaultCodec`, `.encode()` / `.decode()`
on persistence codecs. Cheap, brutal, effective.

Add at the start of the P5 dissolution work. Ratchet the allowed
count down per slice as each artifact family is migrated.

## Source

P5 codec dissolution planning (2026-04-04).
