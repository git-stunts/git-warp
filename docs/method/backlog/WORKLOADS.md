# backlog workloads

This file is intentionally a tombstone.

The previous workload partition scheduled shipped `v17.0.0` residual
cards as live agent work. That made the active backlog look as if v17
still had release-lane items.

The historical partition is archived at
[../../archive/backlog/backlog-workloads-v17-era.md](../../archive/backlog/backlog-workloads-v17-era.md).

Until a fresh workload partition is generated from the current backlog,
use [README.md](README.md) as the live dependency map and staff work from
the active lanes only:

- `inbox/`
- backlog root
- `bad-code/`
- `v18.0.0/`
- `up-next/`
- `v19.0.0/`
- `v20.0.0/`
- `v21.0.0/`
- `cool-ideas/`

Archived v17 notes may be copied or rehomed only by an explicit pull
decision. The archived lane itself is not a live dependency band.
