# WarpGraph Invisible API Surface Docs

**Effort:** M

## Problem

Add `// API Surface` block listing all 40+ dynamically wired methods with source module. Consider generating as a build step. The dynamically composed API surface is invisible to developers reading the source.

## Notes

- File: `src/domain/WarpGraph.js:451-478`
- Low urgency — fold into PRs that touch related files
