# Mermaid Rendering Smoke Test

**Effort:** S

## Problem

Parse all ` ```mermaid ` blocks with `@mermaid-js/mermaid-cli` in CI to catch syntax errors in documentation diagrams before they reach users.

## Notes

- Target: `.github/workflows/ci.yml` or `scripts/`
- Part of P2 CI & Tooling batch
