# API Examples Review Checklist

**Effort:** S

## Problem

Add to `CONTRIBUTING.md`: each `createPatch()`/`commit()` uses own builder, async methods `await`ed, examples copy-pasteable.

## Notes

- Low urgency — fold into PRs that touch related files
