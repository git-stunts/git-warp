# safe-context Phase 1 — The Governor

**Effort:** L

Ship the core context governor as a cross-LLM MCP server + CLI.
Policy-enforced reads, structural outlines, bounded ranges, shell
capture, session state. JS/TS only.

Design exploration in `docs/design/0002-code-nav-tool/`.
Empirical grounding in `~/git/blacklight/LLM_TOKEN_USE.md`.
