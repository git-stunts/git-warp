# Archived Backlog Notes

This directory preserves backlog notes that are useful as historical source
material but are not part of the live backlog inventory.

Archived notes are not rejected by default. They are inactive until a human or
agent explicitly rehomes them into `docs/method/backlog/` with fresh
frontmatter and a current release or lane decision.
