# B12 — Docs-Version-Sync Pre-Commit Check

**Effort:** S
**Origin:** ROADMAP standalone

## Problem

Grep version literals in `.md` files against `package.json` to catch stale version references before they're committed.

## Notes

- Part of P2 CI & Tooling batch
