# B27 — `TrustKeyStore` Pre-Validated Key Cache

**Effort:** S
**Origin:** ROADMAP deferred

## Problem

Cache pre-validated keys in `TrustKeyStore` to avoid repeated validation on hot paths.

## Notes

- **Trigger:** Promote when `verifySignature` appears in any p95 flame graph above 5% of call time
